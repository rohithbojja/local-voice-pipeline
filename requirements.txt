python-dotenv

# ASR — Parakeet TDT MLX (Apple Silicon): https://huggingface.co/animaslabs/parakeet-tdt-0.6b-v3-mlx-4bit
parakeet-mlx

# TTS — OmniVoice (https://huggingface.co/k2-fsa/OmniVoice)
omnivoice

# LLM (local OpenAI-compatible API — Ollama)
httpx

# VAD (Silero) is loaded via torch.hub

# Audio
sounddevice
numpy
soundfile

# Deep learning (torch stack aligned with omnivoice; pin if needed for your platform)
torch
torchaudio

# Web server
fastapi
uvicorn[standard]
websockets
